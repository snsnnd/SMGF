# 实验重构方案

## 1. 重构原因

原先直接按 `S1-S6 × M1/M3/M4/M5/M6/M7` 综合比较，会把过多机制耦合在同一个场景里，导致失败后无法定位问题来源。

例如原先的复杂场景会同时混合：

1. 移动目标
2. 预测导航
3. 多障碍
4. 围捕半径约束
5. 角度覆盖
6. 拓扑调制
7. 安全防撞
8. 输入限幅

因此新的实验结构改为：

1. 单模块验证
2. 双模块或少量机制耦合验证
3. 机制消融验证
4. 难度递增验证
5. 综合挑战验证

## 2. 新实验结构

### Group A: Basic Modules

目标：验证基础模块本身是否合理。

1. `A1` 单智能体无障碍目标到达
2. `A2` 单智能体单障碍绕行
3. `A3` 多智能体无障碍防碰撞

### Group B: Encirclement Geometry

目标：验证围捕构型、角度展开和移动目标开阔环境围捕。

1. `B1` 静态目标均匀初始围捕
2. `B2` 静态目标同侧初始展开
3. `B3` 移动目标无障碍围捕

### Group C: Pressure and Passage

目标：专门验证 `Omega` 环境压力调制。

1. `C1` 宽通道通过
2. `C2` 中等通道通过
3. `C3` 窄通道通过

### Group D: Prediction Navigation

目标：专门验证预测导航 `T_p` 的作用。

1. `D1` 高速目标无障碍
2. `D2` 高速目标 + 单障碍

### Group E: Integrated Challenges

目标：展示系统综合能力，而不是证明单个机制。

1. `E1` 移动目标 + 单障碍
2. `E2` 移动目标 + 少量障碍
3. `E3` 密集障碍 easy
4. `E4` 密集障碍 medium
5. `E5` 密集障碍 hard

## 3. 新评价原则

不再只看 `success_rate`，而是同时看：

1. 几何是否达标
2. 是否无碰撞
3. 是否最终仍达标
4. 是否曾短暂达标
5. 何时达到几何阈值
6. 何时发生碰撞
7. 达标后保持了多久

当前代码已经实现的关键指标包括：

1. `inside_any`
2. `inside_final`
3. `gmax_success`
4. `radius_success`
5. `sigma_success`
6. `obs_collision`
7. `agent_collision`
8. `dwell_success`
9. `dwell_geom_success`
10. `dwell_success_no_collision`
11. `time_to_inside`
12. `time_to_gmax`
13. `time_to_radius`
14. `time_to_full_geom`
15. `success_hold_time`
16. `post_success_violation_count`
17. `last_10s_gmax_mean`
18. `last_10s_radius_error_mean`
19. `last_10s_inside_rate`

## 4. Seed Split Protocol

调参与论文统计必须分离：

1. tuning seeds: `0-9`
2. validation seeds: `10-39`
3. final test seeds: `40-99`

最终论文表格不应直接使用调参种子上的结果。

## 5. 当前代码映射

当前仓库中的实验组名称：

1. `A_basic_modules`
2. `B_encirclement_geometry`
3. `C_pressure_passage`
4. `D_prediction_navigation`
5. `E_integrated_challenges`

可通过以下命令运行：

```bash
/home/aaa/.local/bin/uv run smgf run-group --group A_basic_modules --trials 5 --output outputs/group_A
/home/aaa/.local/bin/uv run smgf run-group --group B_encirclement_geometry --trials 5 --output outputs/group_B
/home/aaa/.local/bin/uv run smgf run-group --group C_pressure_passage --trials 5 --output outputs/group_C
/home/aaa/.local/bin/uv run smgf run-group --group D_prediction_navigation --trials 5 --output outputs/group_D
/home/aaa/.local/bin/uv run smgf run-group --group E_integrated_challenges --trials 5 --output outputs/group_E
```

## 6. 当前限制

虽然实验结构已经重构，但这不表示所有机制都已经被证明成功。当前仍需重点继续推进：

1. `C` 组中 `Omega` 的正向证据
2. `D` 组中 `T_p` 最优区间的显式验证
3. `E` 组中带障碍综合场景的无碰撞成功率提升
