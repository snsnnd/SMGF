# SMGF 研究规划文档

## 1. 项目总定位

SMGF 当前最清晰、最有价值的主线是：

> 状态调制型非保守多智能体引导场方法
> State-Modulated Guidance Field, SMGF

它不是单纯的人工势场，也不是传统强化学习或全局路径规划算法，而是一个面向复杂约束环境的连续分布式多智能体控制框架。

其核心目标是解决：

1. 多智能体在障碍环境中的协同运动；
2. 避障与队形保持之间的冲突；
3. 窄通道、局部拥挤、环境压力变化下的自适应协同；
4. 围捕/包围目标时的半径控制与构型质量评价；
5. 在局部信息条件下实现可解释、可扩展、可实验验证的控制律。

## 2. 当前主干核心公式

SMGF 当前的核心公式为：

`rho_i = eta(Omega_i) * D(tilde(Psi)_i)`

其中：

1. `tilde(Psi)_i`：局部群体构型状态；
2. `Omega_i`：环境压力状态；
3. `eta(Omega_i)`：环境压力调制函数；
4. `D(tilde(Psi)_i)`：构型状态调制函数；
5. `rho_i`：状态调制因子。

最重要的一点是：

> `rho_i` 主要调制拓扑协同项 `F_topo`，而不是调制安全避障项。

因此当前最合理的控制结构是：

`u_i_raw = F_nav^(i) + F_hyb^(i) + F_safe^(i) + rho_i * F_topo^(i) + F_enc^(i)`

再经过速度有界映射：

`u_i = Bnd(u_i_raw)`

这构成了 SMGF 区别于普通势场法的主干特征。

## 3. 主干算法结构

当前项目已经沉淀出的主干模块可概括为七层：

1. 任务层：目标追踪、围捕、避障、窄通道通过、局部拥挤缓解等；
2. 导航场 `F_nav`：提供全局任务驱动力；
3. 混合绕障场 `F_hyb`：结合排斥与切向/旋度绕行；
4. 安全防撞项 `F_safe`：提供局部防撞趋势；
5. 拓扑协同项 `F_topo`：负责局部连接、群体结构与协同行为；
6. 围捕参考项 `F_enc`：提供围捕半径收敛趋势；
7. 状态调制层 `rho_i`：根据环境压力和构型状态连续调制协同强度。

## 4. 当前主干成熟度判断

### 4.1 已经比较成熟的部分

1. 核心概念已经明确：状态调制引导场；
2. 主公式已经稳定：`rho_i = eta(Omega_i) * D(tilde(Psi)_i)`；
3. 控制结构已经成型：导航、绕障、安全、拓扑、围捕、调制；
4. 方法章节已经具备论文主线骨架；
5. 可以围绕“启发式连续调制机制”写成方法型论文。

### 4.2 仍不够成熟的部分

1. 不能直接声称安全保证；
2. 不能直接声称围捕保证；
3. 不能直接声称窄通道通过保证；
4. `Omega_i` 的物理定义还需要加强；
5. `Psi_i` 与角度覆盖之间仍缺少更强实验支撑；
6. 当前 `rho_i` 还是标量调制，方向表达能力不足；
7. 复杂场景综合实验仍未完全打通。

因此当前最合适的论文定位是：

> SMGF 是一种面向复杂约束多智能体协同的状态调制型引导场框架。它通过环境压力与局部构型状态共同调节拓扑协同强度，在避障、围捕和窄通道场景中改善传统固定拓扑或固定势场方法的适应性。

## 5. 后续研究分支总览

SMGF 后续可以发展为一棵研究树：

1. `SMGF-Core`：基础状态调制引导场；
2. `SMGF-Safe / CBF-SMGF`：带安全约束投影的版本；
3. `SMGF-Encirclement`：围捕角度覆盖增强版本；
4. `SMGF-NarrowPassage`：窄通道与队形变形增强版本；
5. `SMGF-Pressure`：多源环境压力建模版本；
6. `SMGF-DCOP`：分布式优化调制版本；
7. `Meta-SMGF`：自动调参与元学习版本；
8. `Geo-SMGF`：几何/拓扑向量场理论增强版本；
9. `Distributed-SMGF`：受限通信与局部信息版本；
10. `MeanField-SMGF`：大规模群体连续体版本；
11. `Robust-SMGF`：不确定性与鲁棒控制版本。
12. `SMGF-EnergyFlow`：状态调制能量-流场耦合版本。

## 6. 推荐的阶段路线

### 第一阶段：稳住主干，完成第一篇论文

重点：

1. 固定控制律结构；
2. 固定 `rho_i` 只调制 `F_topo`；
3. 固定 `Omega_i` 的滤波与有界条件；
4. 完成基础实验、分层实验与消融实验；
5. 形成方法论文闭环。

建议标题方向：

> 基于状态调制引导场的复杂环境多智能体协同控制方法

### 第二阶段：补足理论与构型能力

重点：

1. 围捕角度覆盖增强；
2. 安全投影或 CBF 约束；
3. 窄通道方向性软化；
4. `Omega_i` 的多源物理定义；
5. Lyapunov 或不变集分析。

### 第三阶段：形成 SMGF 系列方法

重点：

1. 自动调参与优化；
2. 分布式优化；
3. 大规模群体建模；
4. 鲁棒性与真实部署。
5. 能量场/流场统一耦合表达。

## 6.1 能量场/流场分支的定位

当前是否需要立刻把系统从“力叠加”彻底重构成“能量场/流场”框架？

当前答案是：**不需要立刻重构主线，但应明确纳入后续规划。**

原因如下：

1. 当前第一阶段的目标是先完成 `SMGF-Core`，即把现有力叠加结构、状态调制主公式和分层实验闭环做扎实；
2. 现有主干已经有清晰的研究价值，没必要在第一篇论文阶段同时推翻控制结构；
3. 如果现在直接切换到能量场/流场框架，会显著增加理论与实现复杂度，反而会延长主干论文闭环时间；
4. 从研究演化路径看，更合理的方式是：
   先把 `SMGF-Core` 作为基础版本完成，
   再把“能量场/流场”作为统一耦合的高级升级方向。

因此，后续规划中建议新增如下分支：

### `SMGF-EnergyFlow`

定位：

> 在保留 `SMGF-Core` 状态调制思想的基础上，将导航、围捕、安全、拓扑等保守项统一为总能量函数的梯度下降方向，同时保留非保守绕障流场作为附加项，从而解决多力直接叠加带来的耦合冲突问题。

可考虑的统一形式为：

`u = -M(x) grad(E(x)) + G(x)`

其中：

1. `E(x)`：由导航、安全、拓扑、围捕、角度覆盖等项组成的总能量；
2. `M(x)`：由 `Psi`、`Omega` 等状态调制得到的状态相关度量或权重矩阵；
3. `G(x)`：保留非保守绕障或旋度流场，用于维持局部绕障能力。

这个分支的意义在于：

1. 从根本上缓解“安全项、拓扑项、围捕项、高速预测项直接叠加”的冲突；
2. 把当前经验性的多力叠加，升级成更统一的能量-流场耦合框架；
3. 为后续各向异性通道软化、角度覆盖统一建模和理论分析提供更自然的表达。

但这一分支的优先级应低于：

1. `SMGF-Core`
2. `SMGF-Pressure`
3. `SMGF-Encirclement`
4. `CBF-SMGF`

也就是说：

> `SMGF-EnergyFlow` 适合作为第二阶段或第三阶段的理论升级方向，而不是当前第一阶段主干论文的直接替代方案。

## 7. 当前阶段判断

结合当前代码、实验结构和已有结果，可以明确判断：

> 我们当前主要处于第一阶段，即“搭建主干”阶段。

当前已经完成的，不是整套理论的最终证明，而是：

1. 方法主干定义；
2. 基本控制结构实现；
3. 分层实验框架重构；
4. 初步正反结果分离；
5. 对主干有效性边界的识别。

## 8. 当前实验已经说明了什么

### 8.1 已有正证据

当前实验已经能够支持以下主张：

1. SMGF 框架不是空想法，基础引导场可以稳定运行；
2. 在开阔、低难度场景中，状态调制协同是可运行的；
3. `S3` 说明调制型拓扑在围捕收缩上比固定拓扑更稳定；
4. `S5-0/B3` 说明移动目标、预测导航、围捕/协同框架在无障碍条件下可以闭环运行；
5. 分层实验已经证明基础模块、角度展开问题、内部碰撞问题可以被区分分析。

### 8.2 尚未被完整证明的部分

当前实验还不能完整支持：

1. `Omega` 调制已经有效提升窄通道通过能力；
2. 完整 SMGF 在复杂障碍环境下整体优于固定拓扑；
3. 构型调制与压力调制已经在综合复杂场景中形成稳定协同收益；
4. 预测导航在高速目标场景中已经形成稳定的最优预测时间区间。

## 9. 第一阶段论文还未完成的部分

当前第一阶段最关键的工作不是继续堆叠新理论，而是补齐实验闭环，证明主干论文中还没完成的部分。

### 9.1 围捕构型部分还需要的实验优化

当前 `F_enc` 只能控制半径，不能保证角度均匀分布，因此必须加强以下验证：

1. `B1`：静态目标均匀初始围捕
   目标：验证半径收敛、半径方差和最终几何稳定性；
2. `B2`：静态目标同侧初始展开
   目标：验证 `Psi` 对角度展开的作用；
3. `B3`：移动目标无障碍围捕
   目标：验证预测导航 + 围捕参考在开阔环境下的组合有效性；
4. 明确比较 `M4/M5/M6/M7/M9` 在以下指标上的差异：
   `final_gmax`、`gmax_reach_time`、`convoy_ratio`、`inside_final`、`radius_error`、`no_collision`。

### 9.2 环境压力部分还需要的实验优化

这是当前主干论文中最关键、也最薄弱的部分。

必须继续强化：

1. `C1`：宽通道通过
   要求：至少证明 `M7` 不比 `M4` 差；
2. `C2`：中等通道通过
   目标：验证 `Omega` 是否能降低碰撞；
3. `C3`：窄通道通过
   目标：作为最终压力场景，而不是一开始就直接使用；
4. 进一步拆分碰撞：
   `obs_collision_rate`、`agent_collision_rate`；
5. 增加入口/通道中段/出口后的碰撞区段定位。

如果 `M7` 在 `C2/C3` 中仍不如 `M4`，则当前论文中不能把 `Omega` 写成已充分验证的正贡献，只能写成：

> 当前结果表明环境压力调制方向具有合理性，但现有标量调制形式在通道约束下仍受群体内部挤压问题限制。

### 9.3 预测导航部分还需要的实验优化

当前 `D` 组还不能证明预测时间的最优区间，因此必须继续：

1. 对 `D1` 做单变量扫描：
   `T_p = 0, 0.3, 0.7, 1.0, 1.3, 1.5`；
2. 固定 `beta_lead`、`r_c`、`u_max`，避免一次改太多参数；
3. 指标至少包含：
   `completion_time`、`current_center_error`、`predicted_center_error`、`input_sat`、`gmax`、`radius_error`；
4. 先在无障碍高速目标场景中证明存在最优区间，再进入 `D2` 单障碍场景。

### 9.4 安全项部分还需要的实验优化

当前 `A3` 已经说明 `F_safe` 有意义，但还不够强。

还需要继续：

1. 用不同初始交叉构型验证 `with/without safe force`；
2. 强化比较 `min_agent_distance` 与 `agent_collision_rate`；
3. 分析为什么 `M7` 虽然比无安全项版本拉开了间距，但仍可能在复杂场景中碰撞。

这部分能支撑主干论文中的谨慎表述：

> `F_safe` 提供局部防撞趋势，但不等价于严格安全保证。

### 9.5 综合场景部分还需要的实验优化

`E` 组不能再拿来证明单个机制，而应作为最终展示组。

当前最合理的顺序是：

1. 先打通 `E1`：移动目标 + 单障碍；
2. 再打通 `E2`：移动目标 + 少量障碍；
3. 然后再逐渐进入 `dense easy / medium / hard`；
4. 不要用 `E` 组失败反推出 `Psi/Omega` 本身无效。

## 10. 第一阶段实验还需要怎么优化

概括起来，当前主干论文实验部分要补齐的重点是：

1. **围捕角度覆盖增强**
   要把“只控半径”与“角度覆盖质量”明确分离；
2. **压力场分级验证**
   先宽通道，再中通道，再窄通道；
3. **预测时间扫描**
   先无障碍，再单障碍；
4. **安全项对照验证**
   明确证明 `F_safe` 的作用边界；
5. **调参与测试数据分离**
   使用：
   `0-9 tuning`、`10-39 validation`、`40-99 final test`；
6. **最终统计样本扩大**
   主结果至少 `30 seeds`，正式论文表格建议 `50-100 seeds`。

## 11. 当前阶段是否需要追求“完美”

当前阶段**不需要过度追求完美**，但必须完成“主干论文成立所需的最低闭环”。

更准确地说：

1. 当前不需要把所有复杂场景全部打通；
2. 当前不需要立即引入所有高级理论分支；
3. 当前不需要把系统做成“保证型控制框架”；
4. 当前最重要的是让第一阶段的主张做到：
   有清晰方法定义、
   有正反对照、
   有可解释结果、
   有边界承认。

也就是说，当前目标不是“完美解决所有问题”，而是：

> 把 `SMGF-Core` 做成一个有明确创新点、有部分正向证据、也能诚实说明局限性的完整方法论文。

## 12. 当前阶段实验还必须补哪些内容

如果只从“第一阶段主干论文是否能成立”来判断，当前实验还必须补的内容可以分为“必须项”和“增强项”。

### 12.1 必须项

这些是当前阶段最应该完成的，缺少它们会导致主干论文证据不足。

1. **围捕角度覆盖增强验证**
   至少完成 `B1/B2/B3`，并明确区分：
   半径收敛、角度覆盖、是否包围、是否碰撞。

2. **压力场分级验证**
   至少完成 `C1/C2/C3` 三档验证，证明：
   `Omega` 的设计方向合理，哪怕暂时不能强证明 `M7` 全面优于 `M4`，也要明确失败来自哪里。

3. **安全项对照实验**
   必须保留 `A3` 和独立的安全力代价对照组，明确说明：
   安全项必要，但会带来任务代价。

4. **预测时间扫描的基础趋势**
   至少在 `D1` 中看出：
   `T_p` 改变会系统性改变行为，哪怕还没形成最终最优区间，也要先有趋势图和趋势表。

5. **调参与测试 seeds 分离**
   这一点已经在框架中实现，但还需要真正跑出：
   `tuning`、`validation` 两个层级，不能只停留在接口层。

### 12.2 增强项

这些不是第一阶段论文成立的硬条件，但会显著提高说服力。

1. `D2` 单障碍高速目标扫描；
2. `E1/E2` 综合挑战中至少出现一个正向示范结果；
3. 更系统的参数敏感性图；
4. 更丰富的轨迹可视化与状态曲线；
5. `30/50/100 seeds` 的正式统计表。

## 13. 当前最准确的主干论文表述

基于当前阶段，最合适的主干论文表述应当是：

> SMGF 是一个以 `rho_i = eta(Omega_i) * D(tilde(Psi)_i)` 为核心的状态调制型多智能体引导场框架。它通过环境压力和局部构型状态自适应调节拓扑协同强度，在保持安全项独立的前提下，提高复杂环境中的避障、围捕和窄通道协同适应性。当前实验已经验证了该框架在开阔和低难度场景中的可运行性，并初步表明其在围捕收缩和协同行为调节上的优势；但在障碍密集、通道约束和高速目标场景下，主导瓶颈仍然是群体内部安全与拓扑软化方式之间的冲突，相关部分仍需进一步实验补强。

## 14. 一句话总结

> SMGF 当前最合理的研究策略不是继续扩大理论宣称，而是先把 `SMGF-Core` 的实验闭环做扎实：完成围捕角度覆盖、环境压力分级验证、预测时间扫描、安全项对照和综合场景分层验证。只有把这些补齐，第一阶段的主干论文才能真正完整成立。
