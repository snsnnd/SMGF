`outputs/` stores active experiment artifacts for the current repository state.

It now contains both:

1. newly generated local runs;
2. retained branch-exploration result sets that are still referenced by active docs.

Examples of currently active result directories:

1. `bb_m62_extended_corridor_probe_v1/`
2. `bc_m43_m62_cross_type_validation_v1/`
3. `bd_m43_m62_dense_tracking_hard_v1/`

Older code snapshots and versioned archive materials still live under `versions/`.
