下面是一份可以直接围绕 **SMGF/F-DDCF 主干论文**展开的完整实验方案。目标不是只画轨迹，而是系统证明：

[
\rho_i=\eta(\Omega_i)D(\tilde{\Psi}_i)
]

确实能让多智能体在**追踪、避障、展开、收缩、窄通道柔性通过**之间实现连续调制。这个方案基于你文稿中已有的系统模型、状态调制因子、混合绕障场、安全防撞项、拓扑协同项和评价指标设计。

------

# 一、实验总目标

主干实验要回答 5 个核心问题：

1. **预测导航项是否能改善移动目标追踪？**
2. **非保守旋度绕障项是否能减少 APF 局部停滞？**
3. **构型序参量 (\tilde{\Psi}_i) 是否能促进群体展开与围捕角度覆盖？**
4. **环境压力参量 (\Omega_i) 是否能让群体在窄通道中自动软化拓扑刚度？**
5. **完整 SMGF 是否优于传统 APF、固定拓扑协同和单状态调制方法？**

最终要证明的不是“所有场景全局最优”，而是：

> **在典型复杂约束环境中，双状态调制机制比固定权重势场和固定拓扑协同更适应围捕、避障与窄通道通过。**

------

# 二、实验总体结构

建议分成 5 层实验。

| 层级    | 实验类型   | 目的                                      |
| ------- | ---------- | ----------------------------------------- |
| Level 1 | 单模块验证 | 验证导航、绕障、(\Psi)、(\Omega) 是否工作 |
| Level 2 | 主任务验证 | 验证追踪、围捕、窄通道通过                |
| Level 3 | 消融实验   | 证明每个核心模块都有贡献                  |
| Level 4 | 参数敏感性 | 证明方法不是极端调参结果                  |
| Level 5 | 鲁棒性测试 | 测试噪声、扰动、目标变向、通信半径变化    |

主干论文至少要完成 Level 1–3。
如果想冲更高水平期刊，建议完成 Level 1–5。

------

# 三、仿真系统设置

## 3.1 智能体模型

采用你文稿中的速度可控模型：

[
\dot p_i(t)=v_{env}(p_i,t)+u_i(t)
]

其中 (u_i(t)) 是主动控制速度，并经过平滑有界算子限制：

[
u_i=Bnd_{u_{max}}(u_i^{raw})
]

初期实验建议先令：

[
v_{env}(p,t)=0
]

即无环境流场。等主干验证稳定后，再加入风场、水流或随机扰动。

------

## 3.2 推荐基础参数

以下参数不是最终值，而是第一轮仿真的合理起点。

| 参数               | 含义                    | 建议初值    |
| ------------------ | ----------------------- | ----------- |
| (N)                | 智能体数量              | 6 或 8      |
| (\Delta t)         | 仿真步长                | 0.02–0.05 s |
| (T)                | 单次实验最大时间        | 80–150 s    |
| (u_{max})          | 主动速度上限            | 1.0         |
| (R_c)              | 通信 / 感知半径         | 5.0–8.0     |
| (d^{safe}_{agent}) | 智能体间安全距离        | 0.4–0.8     |
| (d^{safe}_{obs})   | 障碍物安全距离          | 0.3–0.6     |
| (d_s)              | 障碍影响 / 压力感知距离 | 1.5–3.0     |
| (w_g)              | 导航增益                | 1.0         |
| (k_s)              | 安全防撞增益            | 2.0–4.0     |
| (k_t)              | 拓扑协同增益            | 0.8–2.0     |
| (r_0)              | 邻居期望间距            | 1.2–2.0     |
| (r_c)              | 围捕半径                | 2.0–3.5     |
| (\lambda)          | 旋度绕障系数            | 0.3–1.0     |
| (\delta)           | 构型调制下限            | 0.15–0.4    |
| (\eta_{min})       | 压力调制下限            | 0.15–0.4    |
| (\sigma_\Omega)    | 压力衰减系数            | 2.0–5.0     |
| (q_\Psi)           | 构型调制指数            | 1.0–3.0     |
| (\tau_\Omega)      | 压力滤波时间常数        | 0.3–1.0 s   |
| (T_p)              | 目标预测时间            | 0–1.5 s     |

注意：第一轮不要参数过多变化。先固定一组默认参数跑通完整系统，再做敏感性分析。

------

# 四、对比方法设计

必须有对比组，否则主干创新无法成立。

建议至少设置 7 类方法。

| 编号 | 方法                  | 说明                                 |
| ---- | --------------------- | ------------------------------------ |
| M1   | Traditional APF       | 目标吸引 + 障碍径向斥力              |
| M2   | Bounded APF           | 有界斥力 + 平滑输入限幅              |
| M3   | APF-Curl              | APF + 非保守切向绕障                 |
| M4   | Fixed-Topo Guidance   | 固定拓扑协同，(\rho_i=1)             |
| M5   | SMGF without (\Psi)   | 去掉构型调制，令 (D(\tilde\Psi_i)=1) |
| M6   | SMGF without (\Omega) | 去掉压力调制，令 (\eta(\Omega_i)=1)  |
| M7   | Full SMGF/F-DDCF      | 完整双状态调制方法                   |

如果工作量允许，再加两个增强对比：

| 编号 | 方法                   | 说明                                |
| ---- | ---------------------- | ----------------------------------- |
| M8   | Full SMGF without Curl | 完整调制但去掉旋度绕障，(\lambda=0) |
| M9   | Full SMGF + (F_{ang})  | 加可选角度展开项，只作为增强版本    |

主论文核心比较应放在 M1–M7。
M9 不建议作为主方法，否则会削弱 (\rho_iF_{topo}) 本身的贡献。

------

# 五、评价指标体系

你文稿中已经定义了较完整的指标。实验中建议分成 6 类。

## 5.1 任务成功指标

### 任务成功率

[
R_s=\frac{N_s}{N_{trial}}
]

每个场景至少运行：

[
N_{trial}=50
]

如果想要更强统计可信度，建议：

[
N_{trial}=100
]

------

## 5.2 捕获 / 完成时间

[
T_c=\min{t\mid C(t)=1}
]

围捕任务中，建议捕获条件 (C(t)=1) 定义为同时满足：

1. 目标位于智能体凸包内；
2. 最大角间隙满足：

[
G_{max}\le G_{th}
]

1. 平均半径接近期望围捕半径：

[
|\bar r-r_c|\le \epsilon_r
]

1. 半径方差满足：

[
\sigma_r^2\le \sigma_{th}
]

1. 无障碍碰撞和智能体间碰撞。

建议初始阈值：

| 条件          | 建议阈值                |
| ------------- | ----------------------- |
| (G_{th})      | (120^\circ)–(150^\circ) |
| (\epsilon_r)  | 0.3–0.6                 |
| (\sigma_{th}) | 0.5–1.0                 |

具体阈值要根据 (N)、(r_c)、地图尺度调整。

------

## 5.3 安全指标

### 最小障碍距离

[
d^{min}*{obs}=\min*{i,t}d(p_i(t),O)
]

### 最小智能体间距离

[
d^{min}*{agent}=\min*{i\neq j,t}|p_i(t)-p_j(t)|
]

### 碰撞率

[
R_{coll}=
\frac{#\text{collision trials}}{N_{trial}}
]

其中碰撞包括：

[
d_{obs}<d^{safe}_{obs}
]

或：

[
d_{agent}<d^{safe}_{agent}
]

------

## 5.4 控制代价指标

### 主动控制代价

[
J_u=\sum_{k=0}^{K}\sum_{i=1}^{N}|u_{i,k}|^2\Delta t
]

### 控制平滑性

# [ J_{\Delta u}

\sum_{k=1}^{K}
\sum_{i=1}^{N}
|u_{i,k}-u_{i,k-1}|^2
]

### 输入接近上限比例

# [ R_{bnd}

\frac{#{(i,k)\mid |u_{i,k}|\ge 0.95u_{max}}}
{N(K+1)}
]

这能证明 SMGF 不是靠频繁顶满执行器来获得性能。

------

## 5.5 围捕质量指标

### 最大角间隙

[
G_{max}=\max_i(\theta_{i+1}-\theta_i)
]

这是判断是否“同侧圆弧聚集”的关键指标。

### 半径方差

[
\sigma_r^2=
\frac{1}{N}
\sum_{i=1}^{N}
(|p_i-p_T|-\bar r)^2
]

### 当前目标中心误差

[
e_T=|p_c-p_T|
]

### 预测目标中心误差

[
e_{\hat T}=|p_c-\hat p_T|
]

如果 (e_{\hat T}<e_T)，说明系统更偏向预测目标点形成前导包围。

------

## 5.6 状态调制解释指标

这是 SMGF 主干最重要的可解释性证据。

必须记录并绘制：

[
\Psi(t),\quad \tilde{\Psi}_i(t),\quad \Omega_i(t),\quad \rho_i(t)
]

建议画：

1. 全体智能体平均值：

[
\bar \Psi(t),\quad \bar \Omega(t),\quad \bar \rho(t)
]

1. 最大 / 最小范围：

[
\Omega_{min}(t),\Omega_{max}(t)
]

[
\rho_{min}(t),\rho_{max}(t)
]

1. 典型智能体的单独曲线。

这些曲线要和轨迹图对应解释：

- 进入窄通道时，(\Omega) 上升；
- (\eta(\Omega)) 下降；
- (\rho) 下降；
- 拓扑刚度降低；
- 群体变形通过通道；
- 离开通道后，(\Omega) 下降，(\rho) 恢复。

------

# 六、核心实验场景设计

## 场景 S1：单智能体局部绕障验证

### 目的

验证非保守旋度项是否能缓解传统 APF 的局部停滞。

### 设置

- 1 个智能体；
- 1 个目标点；
- 1 个圆形障碍物位于智能体和目标之间；
- 初始点、障碍物中心、目标近似共线。

### 对比方法

- M1 Traditional APF；
- M2 Bounded APF；
- M3 APF-Curl。

### 指标

| 指标         | 目的             |
| ------------ | ---------------- |
| 到达率       | 是否成功绕过障碍 |
| 完成时间     | 是否更快         |
| 停滞次数     | 是否减少局部极小 |
| 最小障碍距离 | 是否安全         |
| 路径长度     | 是否绕行过度     |

### 预期要验证的现象

普通 APF 在障碍前方容易停滞；加入切向绕障后，轨迹绕过障碍并到达目标。

------

## 场景 S2：多智能体同侧展开实验

### 目的

验证 (\Psi) 构型调制是否能改善同侧追踪和围捕角度覆盖。

### 设置

- (N=6) 或 (8)；
- 目标初始在前方；
- 所有智能体从目标同一侧出发；
- 无障碍或少量远离障碍；
- 目标静止或低速移动。

### 对比方法

- M4 Fixed-Topo Guidance；
- M5 SMGF without (\Psi)；
- M6 SMGF without (\Omega)；
- M7 Full SMGF。

此场景中 (\Omega) 作用较弱，主要看 (\Psi)。

### 指标

| 指标                  | 目的         |
| --------------------- | ------------ |
| (G_{max})             | 角度覆盖质量 |
| (\Psi(t))             | 构型展开过程 |
| 捕获时间 (T_c)        | 围捕效率     |
| 半径方差 (\sigma_r^2) | 半径一致性   |
| 凸包包含率            | 是否真正包围 |

### 预期要验证的现象

Full SMGF 应该比 fixed-topo 或 no-(\Psi) 方法更快降低 (\Psi)，并显著降低 (G_{max})。

------

## 场景 S3：固定目标围捕收缩实验

### 目的

验证群体展开后，构型调制能否降低拓扑刚度，使智能体向围捕半径收缩。

### 设置

- 目标静止；
- 智能体初始分布较分散；
- 无障碍；
- 启用 (F_{enc})。

### 对比方法

- M4 Fixed-Topo；
- M5 without (\Psi)；
- M7 Full SMGF。

### 指标

| 指标           | 目的             |
| -------------- | ---------------- |
| (\bar r(t))    | 是否接近期望半径 |
| (\sigma_r^2)   | 半径均匀性       |
| (G_{max})      | 周向覆盖         |
| (J_u)          | 控制代价         |
| (J_{\Delta u}) | 控制平滑性       |

### 预期要验证的现象

固定拓扑可能保持过强间距，收缩不足；Full SMGF 在分布均匀后降低拓扑刚度，使围捕半径更接近 (r_c)。

------

## 场景 S4：窄通道柔性通过实验

### 目的

验证 (\Omega) 环境压力调制是否有效。

这是主干论文最关键实验之一。

### 设置

- 两个或多个障碍物构成窄通道；
- 通道宽度略大于单智能体安全直径，但小于固定编队宽度；
- 目标位于通道另一侧；
- 多智能体需要穿过通道追踪目标。

### 通道宽度建议

设智能体安全间距为 (d^{safe}_{agent})，通道宽度设置为：

[
W \in [1.5d^{safe}*{agent}, 3.0d^{safe}*{agent}]
]

做多个宽度：

[
W={1.5,2.0,2.5,3.0}d^{safe}_{agent}
]

### 对比方法

- M4 Fixed-Topo；
- M5 without (\Psi)；
- M6 without (\Omega)；
- M7 Full SMGF。

### 指标

| 指标              | 目的               |
| ----------------- | ------------------ |
| 通道通过率        | 核心指标           |
| 通过时间          | 效率               |
| 通道内拥堵时间    | 是否卡住           |
| (d^{min}_{obs})   | 障碍安全           |
| (d^{min}_{agent}) | 内部安全           |
| (\Omega(t))       | 是否感知压力       |
| (\rho(t))         | 是否降低拓扑刚度   |
| (R_{bnd})         | 是否靠极限输入硬过 |

### 预期要验证的现象

进入窄通道时，Full SMGF 的 (\Omega_i) 上升，(\rho_i) 下降，拓扑项被软化，群体由横向展开转为纵向或柔性队列通过。

------

## 场景 S5：障碍密集环境下移动目标追踪

### 目的

验证完整复杂环境下的综合性能。

### 设置

- 随机放置 8–15 个圆形障碍物；
- 存在 1–2 个窄通道；
- 移动目标以恒定速度或缓慢变向运动；
- 多智能体从同侧或随机位置出发。

### 目标运动方式

建议设置 3 类：

1. 匀速直线：

[
v_T=\text{constant}
]

1. 正弦机动：

[
v_T(t)=v_0[\cos(\omega t),\sin(\omega t)]
]

1. 突然转向：

[
v_T(t)=
\begin{cases}
v_1, & t<t_s\
v_2, & t\ge t_s
\end{cases}
]

### 对比方法

- M1 APF；
- M3 APF-Curl；
- M4 Fixed-Topo；
- M5 without (\Psi)；
- M6 without (\Omega)；
- M7 Full SMGF。

### 指标

| 指标                    | 目的          |
| ----------------------- | ------------- |
| 成功率 (R_s)            | 总体性能      |
| 捕获时间 (T_c)          | 效率          |
| 碰撞率 (R_{coll})       | 安全          |
| (d^{min}_{obs})         | 障碍裕度      |
| (d^{min}_{agent})       | 内部安全      |
| (G_{max})               | 围捕质量      |
| (J_u), (J_{\Delta u})   | 控制代价      |
| (e_T,e_{\hat T})        | 当前/前导包围 |
| (\Psi,\Omega,\rho) 曲线 | 可解释性      |

------

## 场景 S6：前导包围 / 高速目标实验

### 目的

验证预测导航项 (\hat p_T=p_T+T_pv_T) 的作用。

### 设置

- 开阔环境或少量障碍；
- 目标速度较快；
- 多智能体需要提前拦截，而不是追尾。

### 对比方法

- (T_p=0)：无预测；
- (T_p=0.5)；
- (T_p=1.0)；
- (T_p=1.5)。

### 指标

| 指标         | 目的                 |
| ------------ | -------------------- |
| 捕获时间     | 预测是否加快拦截     |
| (e_T)        | 当前目标中心误差     |
| (e_{\hat T}) | 预测点中心误差       |
| 追尾比例     | 是否长期落后目标     |
| 控制代价     | 是否过度预测导致振荡 |

### 预期要验证的现象

适当 (T_p) 能减少追尾，提高拦截效率；过大 (T_p) 可能导致超前过度和振荡。

------

## 场景 S7：非凸 / U 型障碍失败边界实验

### 目的

明确方法适用边界，避免过度宣称。

### 设置

- U 型障碍或凹形障碍；
- 目标位于 U 型内部或外部；
- 智能体需要绕出或进入。

### 对比方法

- M1 APF；
- M3 APF-Curl；
- M7 Full SMGF。

### 指标

| 指标         | 目的             |
| ------------ | ---------------- |
| 成功率       | 是否能逃逸       |
| 停滞时间     | 是否陷入局部循环 |
| 轨迹循环次数 | 是否产生极限环   |
| 最小障碍距离 | 是否安全         |
| 失败案例轨迹 | 明确限制         |

### 论文中的作用

这个实验不是为了证明 SMGF 万能，而是为了说明：

> SMGF 改善局部绕障，但不保证任意强非凸环境下全局路径完备性。

这和你文稿中的理论定位一致。

------

# 七、消融实验设计

消融实验是整篇论文最关键部分。

## 7.1 消融矩阵

| 实验编号 | (\Psi) 调制 | (\Omega) 调制 | Curl | 拓扑项 | 目的           |
| -------- | ----------- | ------------- | ---- | ------ | -------------- |
| A0       | 否          | 否            | 否   | 否     | 传统 APF       |
| A1       | 否          | 否            | 是   | 否     | 验证 Curl      |
| A2       | 否          | 否            | 是   | 固定   | 验证固定拓扑   |
| A3       | 是          | 否            | 是   | 调制   | 验证构型调制   |
| A4       | 否          | 是            | 是   | 调制   | 验证压力调制   |
| A5       | 是          | 是            | 否   | 调制   | 验证旋度项贡献 |
| A6       | 是          | 是            | 是   | 调制   | 完整 SMGF      |

## 7.2 需要证明的具体结论

| 对比     | 需要证明                           |
| -------- | ---------------------------------- |
| A1 vs A0 | Curl 减少障碍前停滞                |
| A3 vs A2 | (\Psi) 改善角度覆盖                |
| A4 vs A2 | (\Omega) 改善窄通道通过            |
| A6 vs A3 | 双状态比单独 (\Psi) 更适应障碍压力 |
| A6 vs A4 | 双状态比单独 (\Omega) 更适合围捕   |
| A6 vs A5 | Curl 对复杂障碍绕行仍有贡献        |

------

# 八、参数敏感性实验

参数敏感性不需要所有参数都扫，否则工作量太大。建议选 5 个最关键参数。

## 8.1 建议扫描参数

| 参数            | 作用                 | 扫描范围              |
| --------------- | -------------------- | --------------------- |
| (\lambda)       | 旋度绕障强度         | 0, 0.3, 0.6, 0.9, 1.2 |
| (\eta_{min})    | 高压力下最低拓扑刚度 | 0.1, 0.2, 0.3, 0.5    |
| (\delta)        | 构型调制下限         | 0.1, 0.2, 0.3, 0.5    |
| (\sigma_\Omega) | 压力衰减速度         | 1, 2, 3, 5, 8         |
| (q_\Psi)        | 构型调制非线性       | 1, 2, 3, 4            |

## 8.2 输出图

建议画：

1. 成功率热力图；
2. (G_{max}) 热力图；
3. 通道通过率热力图；
4. 碰撞率热力图；
5. 控制代价热力图。

核心目标是证明：

> 在一段参数范围内，Full SMGF 都能保持较好性能，而不是只在单个精调参数点有效。

------

# 九、鲁棒性实验

主干论文如果篇幅够，建议加入简化鲁棒性测试。

## 9.1 噪声类型

| 不确定性         | 建议形式                     |
| ---------------- | ---------------------------- |
| 目标位置噪声     | (p_T^{obs}=p_T+\epsilon_p)   |
| 目标速度估计误差 | (v_T^{obs}=v_T+\epsilon_v)   |
| 障碍物位置误差   | (c_o^{obs}=c_o+\epsilon_o)   |
| 智能体定位噪声   | (p_i^{obs}=p_i+\epsilon_i)   |
| 环境流场扰动     | (v_{env}=v_0+\epsilon_{env}) |
| 通信延迟         | 使用 (p_j(t-\tau_d))         |

## 9.2 噪声强度

设置低、中、高三档：

| 档位   | 噪声标准差  |
| ------ | ----------- |
| Low    | 1% 地图尺度 |
| Medium | 3% 地图尺度 |
| High   | 5% 地图尺度 |

## 9.3 指标

- 成功率随噪声强度变化；
- 碰撞率随噪声强度变化；
- (G_{max}) 随噪声变化；
- (\Psi,\Omega,\rho) 是否抖动；
- 控制平滑性 (J_{\Delta u})。

------

# 十、统计分析方法

每个方法、每个场景建议运行：

[
50\text{ 到 }100
]

个随机种子。

随机内容包括：

- 初始智能体位置；
- 目标初始位置；
- 目标运动方向；
- 障碍物微小扰动；
- 噪声序列。

## 10.1 汇报方式

每个指标报告：

[
mean \pm std
]

同时建议给出：

- median；
- interquartile range；
- success rate；
- boxplot；
- trajectory examples。

## 10.2 显著性检验

如果你想增强论文说服力，可以使用：

- Mann–Whitney U test；
- Wilcoxon signed-rank test；
- t-test，前提是数据近似正态。

重点比较：

[
\text{Full SMGF}
\quad vs \quad
\text{Fixed-Topo}
]

[
\text{Full SMGF}
\quad vs \quad
\text{without }\Psi
]

[
\text{Full SMGF}
\quad vs \quad
\text{without }\Omega
]

------

# 十一、必须输出的图表

## 11.1 轨迹图

每个主要场景至少画：

1. APF 轨迹；
2. Fixed-Topo 轨迹；
3. Full SMGF 轨迹。

轨迹图中应标注：

- 障碍物；
- 目标路径；
- 智能体路径；
- 起点；
- 终点；
- 捕获时刻；
- 通道区域。

------

## 11.2 状态调制曲线

必须画：

[
\Psi(t),\Omega(t),\rho(t)
]

尤其在窄通道场景中，要展示：

- 进入通道前：(\Omega) 低，(\rho) 高；
- 通道内：(\Omega) 高，(\rho) 低；
- 离开通道后：(\Omega) 下降，(\rho) 恢复。

这是证明“连续状态切换”的关键证据。

------

## 11.3 性能柱状图 / 箱线图

建议画：

- 成功率；
- 捕获时间；
- (G_{max})；
- 最小障碍距离；
- 最小智能体距离；
- 控制代价；
- 通道通过率。

------

## 11.4 消融热力图

建议画：

- 参数 (\eta_{min}) vs (\sigma_\Omega) 对通道通过率的影响；
- 参数 (\delta) vs (q_\Psi) 对 (G_{max}) 的影响；
- (\lambda) 对停滞率和路径长度的影响。

------

# 十二、论文中可以组织成的实验章节

建议第 5 章或实验部分这样写：

## 5.1 Experimental Setup

- 智能体模型；
- 参数设置；
- 障碍物与目标设置；
- 对比方法；
- 评价指标。

## 5.2 Module Verification

- 预测导航验证；
- 混合绕障验证；
- 构型调制验证；
- 环境压力调制验证。

## 5.3 Ablation Study

- 去掉 (\Psi)；
- 去掉 (\Omega)；
- 去掉 Curl；
- 固定拓扑；
- 完整 SMGF。

## 5.4 Complex Scenario Evaluation

- 移动目标；
- 密集障碍；
- 窄通道；
- 前导包围。

## 5.5 Parameter Sensitivity

- 关键参数扫描；
- 热力图。

## 5.6 Robustness Test

- 噪声；
- 目标变向；
- 环境流场；
- 通信半径变化。

## 5.7 Failure Case and Limitation

- U 型障碍；
- 强非凸区域；
- 极端窄通道；
- 局部通信断裂。

这一节很重要。它能证明你的论文没有过度声称。

------

# 十三、实验阶段推进顺序

建议不要一开始就做完整复杂场景。按下面顺序推进。

## 阶段 1：最小系统跑通

目标：单智能体 + 目标 + 障碍物。

完成：

- (F_{nav})；
- (F_{rep})；
- (F_{curl})；
- 输入限幅；
- 轨迹可视化。

通过标准：

- 能绕开单个障碍；
- 不出现数值爆炸；
- 控制输入有界。

------

## 阶段 2：多智能体基础系统

目标：无障碍情况下完成展开与围捕。

完成：

- (F_{safe})；
- (F_{topo})；
- (F_{enc})；
- (\Psi,\tilde\Psi,D(\tilde\Psi))。

通过标准：

- 智能体不碰撞；
- 能围绕目标形成基本分布；
- (G_{max}) 下降；
- (\Psi(t)) 从高到低变化。

------

## 阶段 3：加入环境压力调制

目标：窄通道柔性通过。

完成：

- (\phi_i)；
- (\Omega_i)；
- (\eta(\Omega_i))；
- (\rho_i)。

通过标准：

- 进入通道时 (\Omega) 上升；
- (\rho) 下降；
- 群体变形通过；
- 离开后 (\rho) 恢复。

------

## 阶段 4：完整移动目标围捕

目标：复杂场景下完整任务。

完成：

- 预测目标点 (\hat p_T)；
- 移动目标；
- 障碍物密集场景；
- 完整指标统计。

通过标准：

- Full SMGF 在成功率、(G_{max})、通道通过率上优于主要基线。

------

## 阶段 5：大规模统计与论文图表

目标：形成论文结果。

完成：

- 每组 50–100 次随机实验；
- 全部指标统计；
- 消融实验；
- 参数敏感性；
- 失败案例。

------

# 十四、最小可发表实验组合

如果你想先做一版主干论文，最小组合应该包括：

1. 单智能体绕障实验；
2. 多智能体同侧展开实验；
3. 窄通道通过实验；
4. 移动目标围捕实验；
5. 完整消融实验；
6. 参数敏感性简版。

这已经可以支撑一篇完整方法论文。

------

# 十五、强版本实验组合

如果目标是冲更高水平期刊，建议加入：

1. 复杂障碍密集环境；
2. 多目标速度实验；
3. 目标突然变向；
4. 噪声和通信延迟；
5. U 型障碍失败边界；
6. 真实机器人或高保真仿真；
7. CBF 安全过滤对照。

但 CBF 最好作为后续安全分支，不建议在主干第一版中过度展开，否则会掩盖 SMGF 本身贡献。

------

# 十六、关键判断标准

主干实验是否成功，主要看这几件事：

| 目标          | 成功标准                                      |
| ------------- | --------------------------------------------- |
| (\Psi) 有效   | Full SMGF 的 (G_{max}) 明显低于 no-(\Psi)     |
| (\Omega) 有效 | Full SMGF 的窄通道通过率明显高于 no-(\Omega)  |
| Curl 有效     | APF-Curl 的停滞率低于 APF                     |
| 双状态有效    | Full SMGF 综合优于单独 (\Psi) 或单独 (\Omega) |
| 安全项必要    | Full SMGF 保持较好 (d^{min}_{agent})          |
| 输入合理      | (R_{bnd}) 不应过高                            |
| 控制平滑      | (J_{\Delta u}) 不应显著恶化                   |
| 可解释性成立  | (\Psi,\Omega,\rho) 曲线与行为切换一致         |

如果这些结果成立，主干方向就站得住。

------

# 十七、最重要的实验结论模板

最终你希望得到这样的结论：

> 在开阔区域中，SMGF 通过构型序参量保持较强拓扑展开能力，使智能体从同侧追踪状态逐渐转入目标周向分布状态；在群体分布趋于均匀后，构型调制降低拓扑刚度，使群体向期望围捕半径收缩。进入障碍密集或窄通道区域时，环境压力参量升高，压力调制函数降低拓扑协同刚度，使群体由刚性协同转为柔性通过。相比传统 APF、固定拓扑协同和单状态调制方法，完整 SMGF 在围捕角度覆盖、窄通道通过率和综合任务成功率方面表现更优，同时保持主动输入有界。

这就是主干论文实验部分要支撑的核心结论。