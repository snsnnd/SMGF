# 安全力代价对照实验报告

## 1. 实验目的

本组实验专门回答以下问题：

> 在不把“是否发生智能体碰撞”当作唯一结论的前提下，安全力 `F_safe` 是否会带来明显的任务性能代价？

为此，构造对照：

1. `M7`: Full SMGF with safe force
2. `M10`: Full SMGF without safe force

## 2. 实验组

当前已新增实验组：

`F_safe_tradeoff`

包含场景：

1. `a3_multi_agent_safety_crossing`
2. `b3_moving_target_open_encirclement`
3. `s4_narrow_passage_easy`
4. `s6_fast_target`
5. `d2_fast_target_single_obstacle`

## 3. 已运行结果

输出目录：

`outputs/group_F_safe_tradeoff/`

本轮使用：

1. `trials = 5`
2. `seed_start = 0`

## 4. 结果解读

### 4.1 A3：安全项是必要的

在 `a3_multi_agent_safety_crossing` 中：

1. `M10` 的最小智能体距离约为 `0.0006`
2. `M7` 的最小智能体距离约为 `0.472`

说明：

1. 去掉安全项后，智能体几乎完全重合；
2. 安全项显著提高了群体内部最小间距；
3. 但两者都还发生碰撞，说明当前 `F_safe` 仍不足以单独解决复杂交汇场景中的内部碰撞。

### 4.2 B3：在开阔移动围捕场景中，安全项代价很小

在 `b3_moving_target_open_encirclement` 中：

1. `M7` 与 `M10` 指标几乎一致；
2. 两者 `success_rate = 1.0`；
3. 两者 `collision_rate = 0.0`。

说明：

1. 在开阔、无障碍、低冲突场景中，安全力并不会明显拖慢任务；
2. 安全项的“副作用”不是普遍存在，而是与场景耦合强相关。

### 4.3 S4-easy：去掉安全项会让几何更激进，但内部安全完全崩掉

在 `s4_narrow_passage_easy` 中：

1. `M10` 的 `min_agent_distance_mean` 约为 `0.0008`
2. `M7` 的 `min_agent_distance_mean` 约为 `0.198`
3. `M10` 的 `gmax_mean` 更大，约 `130.9 deg`
4. `M7` 的 `gmax_mean` 更小，约 `76.6 deg`

说明：

1. 去掉安全项并没有让通道行为“整体更好”；
2. 相反，它导致群体严重压缩，几乎失去内部间距；
3. `M7` 虽然仍失败，但在群体几何与间距上比 `M10` 更合理。

### 4.4 D1/D2：高速目标场景里，安全项并不是导致失败的主因

在 `s6_fast_target` 和 `d2_fast_target_single_obstacle` 中：

1. `M7` 和 `M10` 都失败；
2. 两者都出现 `agent_collision_rate = 1.0`；
3. 若忽略碰撞，仅从 `completion_time_mean` 看，`M10` 有时略快；
4. 但 `M7` 在最小智能体间距上始终更大。

说明：

1. 当前高速目标场景下的瓶颈并不是“有安全项就一定坏”；
2. 更准确地说，是现有的安全项、拓扑项、围捕项与高速预测行为之间耦合还不够好；
3. 去掉安全项只能让系统更激进，但不能真正解决失败机制。

## 5. 当前可下的结论

当前实验**不能**直接支持以下说法：

> 安全力会导致整个效果变差。

当前实验**可以**支持以下更准确的表述：

> 在若干复杂场景中，安全力会带来一定的任务性能代价，例如增加保守性、减弱收缩速度、影响几何收敛；但与此同时，安全力对维持最小群体间距仍然是必要的。当前问题不在于“是否需要安全力”，而在于现有安全项与导航、拓扑和围捕项之间的耦合方式仍需进一步优化。

## 6. 一句话总结

> 安全力不是单纯的“负担”，而是一个带来明确安全收益、同时也会引入任务代价的必要模块；当前实验表明，它的代价已经可观测，但其收益同样不可忽略，因此后续优化重点应放在“降低安全力的副作用”，而不是简单删除安全力。
