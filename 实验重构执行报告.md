# 实验重构执行报告

## 1. 重构目标

本次重构的目标不是继续沿用原有的 `S1-S6 × 多方法` 直接综合比较模式，而是将实验体系改造成分层验证结构，使每个实验组对应更明确的科学问题。

新的结构为：

1. Group A：基础模块验证
2. Group B：围捕构型验证
3. Group C：环境压力与通道变形验证
4. Group D：预测导航验证
5. Group E：综合挑战验证

## 2. 已完成的代码重构

### 2.1 新实验组运行入口

新增命令：

```bash
/home/aaa/.local/bin/uv run smgf run-group --group A_basic_modules --trials 5 --output outputs/group_A
```

当前支持的实验组：

1. `A_basic_modules`
2. `B_encirclement_geometry`
3. `C_pressure_passage`
4. `D_prediction_navigation`
5. `E_integrated_challenges`

### 2.2 新场景层级

新增或重组的场景包括：

1. `a1_single_goal_reach`
2. `a3_multi_agent_safety_crossing`
3. `b1_static_uniform_encirclement`
4. `b3_moving_target_open_encirclement`
5. `s4_narrow_passage_easy`
6. `s4_narrow_passage_medium`
7. `s4_narrow_passage_hard`
8. `s5_tracking_stage0`
9. `s5_tracking_stage1`
10. `s5_tracking_stage2`
11. `d2_fast_target_single_obstacle`
12. `e1_tracking_single_obstacle`
13. `e2_tracking_sparse_obstacles`

### 2.3 新对比方法

新增：

1. `M9`：Full SMGF + weak angular regularization
2. `M10`：Full SMGF without safe force

这两个方法分别服务于：

1. `A3` 安全项必要性验证
2. `B2/B3` 角度展开增强验证

### 2.4 新指标体系

在原有指标外，增加了：

1. `obs_collision`
2. `agent_collision`
3. `dwell_geom_success`
4. `dwell_success_no_collision`
5. `time_to_inside`
6. `time_to_gmax`
7. `time_to_radius`
8. `time_to_full_geom`
9. `success_hold_time`
10. `post_success_violation_count`
11. `last_10s_gmax_mean`
12. `last_10s_radius_error_mean`
13. `last_10s_inside_rate`

## 3. 本轮验证方式

为了验证重构后的实验框架确实可以工作，本轮进行了以下 smoke runs：

1. Group A：`outputs/group_A_smoke/`
2. Group B：`outputs/group_B_smoke/`
3. Group C：`outputs/group_C_smoke/`
4. Group D：`outputs/group_D_smoke/`

同时保留了一个聚焦套件：

1. `outputs/optimized_suite_v1/`

## 4. 关键观察

### 4.1 Group A 结果符合“基础模块验证”预期

1. `A1` 中 `M1/M2` 均成功到达目标
2. `A2` 中 `M3` 能提供更大的障碍安全裕度，但平滑性仍需继续优化
3. `A3` 中 `M10` 与 `M7` 都失败，但 `M7` 的最小智能体距离显著大于 `M10`

这说明：

1. 基础导航没问题
2. Curl 有可见作用
3. `F_safe` 的确在拉开智能体间距，但当前还不足以完全阻止聚集碰撞

### 4.2 Group B 成功把“角度展开”和“内部碰撞”分离出来了

从 `outputs/group_B_smoke/summary_metrics.csv` 可见：

1. `b3_moving_target_open_encirclement` 中 `M4/M7/M9` 都无碰撞成功
2. `M9` 在该场景下明显降低了 `gmax_mean`
3. `s2_same_side_expansion` 中 `M9` 把 `gmax_mean` 压低到约 `60 deg`，但仍发生内部碰撞

这非常重要，因为它说明：

1. 原先把 `S2` 全失败简单解释为“Psi 无效”是不够的
2. 现在可以区分：
   角度展开是否改善
   与是否发生内部碰撞

也就是说，B 组重构已经达到目的。

### 4.3 Group C 证明了当前 `Omega` 问题的真正位置

从 `outputs/group_C_smoke/summary_metrics.csv` 可见：

1. `C1/C2` 多数失败由 `agent_collision_rate = 1.0` 主导
2. `obs_collision_rate` 在很多配置下反而不高
3. 当前 S4 的主要问题不是“没识别到通道”，而是“通过时和通过后群体内部挤压”

因此，新的 C 组结构已经比原先的混合 S4 更有解释力。

### 4.4 Group D 把预测问题单独剥离出来了

从 `outputs/group_D_smoke/summary_metrics.csv` 可见：

1. `s6_fast_target` 与 `d2_fast_target_single_obstacle` 当前都没有成功
2. 但失败机制现在已经和 `Omega`、密集障碍问题分离开了
3. 这意味着之后做 `T_p` 扫描时，结论会更清楚，不会被 S5/S4 的复杂障碍问题污染

## 5. 这次重构带来的直接收益

这次重构最大的收益不是“立刻把所有实验调成功”，而是让问题定位能力大幅提升。

现在可以更明确回答：

1. 基础导航是否正常：可以
2. Curl 是否有作用：有
3. Safe force 是否必要：是
4. 角度正则项是否能改善 `Gmax`：能
5. 当前 S4 的失败主因是什么：主要是 `agent_collision`
6. 当前 S5 的问题在哪一层开始出现：从单障碍阶段就开始出现

## 6. 现阶段仍未完成的部分

虽然实验结构已经重构完成，但以下内容仍然需要继续推进：

1. `C` 组中让 `M7` 真正优于 `M4`
2. `D` 组中形成明确的 `T_p` 最优区间证据
3. `E` 组中让 integrated challenge 出现稳定正结果
4. 按 `0-9 / 10-39 / 40-99` 种子分层进行正式统计

## 7. 当前结论

可以明确说：

> 你的实验体系已经不再是“场景混杂导致无法解释失败”的状态。现在代码和数据已经完成了从综合混合验证到分层机制验证的重构，后续调参和论文撰写会更有针对性，也更符合审稿中对可解释实验设计的要求。
